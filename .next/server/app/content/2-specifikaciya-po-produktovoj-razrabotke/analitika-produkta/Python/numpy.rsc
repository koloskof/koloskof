1:"$Sreact.fragment"
2:I[7555,[],""]
3:I[1295,[],""]
5:I[9665,[],"OutletBoundary"]
7:I[4911,[],"AsyncMetadataOutlet"]
9:I[9665,[],"ViewportBoundary"]
b:I[9665,[],"MetadataBoundary"]
c:"$Sreact.suspense"
e:I[8393,[],""]
:HL["/_next/static/media/028c0d39d2e8f589-s.p.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
:HL["/_next/static/media/5b01f339abf2f1a5.p.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
:HL["/_next/static/css/e27041d1401e0af5.css","style"]
0:{"P":null,"b":"p4sgdc3utZK-wUSIC-oba","p":"","c":["","content","2-specifikaciya-po-produktovoj-razrabotke","analitika-produkta","python","numpy",""],"i":false,"f":[[["",{"children":["content",{"children":[["slug","2-specifikaciya-po-produktovoj-razrabotke/analitika-produkta/python/numpy","c"],{"children":["__PAGE__",{}]}]}]},"$undefined","$undefined",true],["",["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/_next/static/css/e27041d1401e0af5.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]],["$","html",null,{"lang":"ru","className":"dark h-full","children":["$","body",null,{"className":"__variable_fb8f2c __variable_f910ec antialiased h-full bg-background flex flex-col","children":[["$","div",null,{"className":"flex-1","children":["$","$L2",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L3",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[[["$","title",null,{"children":"404: This page could not be found."}],["$","div",null,{"style":{"fontFamily":"system-ui,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif,\"Apple Color Emoji\",\"Segoe UI Emoji\"","height":"100vh","textAlign":"center","display":"flex","flexDirection":"column","alignItems":"center","justifyContent":"center"},"children":["$","div",null,{"children":[["$","style",null,{"dangerouslySetInnerHTML":{"__html":"body{color:#000;background:#fff;margin:0}.next-error-h1{border-right:1px solid rgba(0,0,0,.3)}@media (prefers-color-scheme:dark){body{color:#fff;background:#000}.next-error-h1{border-right:1px solid rgba(255,255,255,.3)}}"}}],["$","h1",null,{"className":"next-error-h1","style":{"display":"inline-block","margin":"0 20px 0 0","padding":"0 23px 0 0","fontSize":24,"fontWeight":500,"verticalAlign":"top","lineHeight":"49px"},"children":404}],["$","div",null,{"style":{"display":"inline-block"},"children":["$","h2",null,{"style":{"fontSize":14,"fontWeight":400,"lineHeight":"49px","margin":0},"children":"This page could not be found."}]}]]}]}]],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]}],["$","footer",null,{"className":"border-t border-border mt-auto","children":["$","div",null,{"className":"container mx-auto px-4 py-8 max-w-4xl","children":["$","div",null,{"className":"text-center text-sm text-muted-foreground","children":["$","p",null,{"children":"КОЛОСКОВ™"}]}]}]}]]}]}]]}],{"children":["content",["$","$1","c",{"children":[null,["$","$L2",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L3",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":[["slug","2-specifikaciya-po-produktovoj-razrabotke/analitika-produkta/python/numpy","c"],["$","$1","c",{"children":[null,["$","$L2",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L3",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":["__PAGE__",["$","$1","c",{"children":["$L4",null,["$","$L5",null,{"children":["$L6",["$","$L7",null,{"promise":"$@8"}]]}]]}],{},null,false]},null,false]},null,false]},null,false],["$","$1","h",{"children":[null,[["$","$L9",null,{"children":"$La"}],["$","meta",null,{"name":"next-size-adjust","content":""}]],["$","$Lb",null,{"children":["$","div",null,{"hidden":true,"children":["$","$c",null,{"fallback":null,"children":"$Ld"}]}]}]]}],false]],"m":"$undefined","G":["$e",[]],"s":false,"S":true}
a:[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]
6:null
f:I[8175,[],"IconMark"]
8:{"metadata":[["$","title","0",{"children":"КОЛОСКОВ.РФ: Платформизация экономики"}],["$","meta","1",{"name":"description","content":"Размышления о финтехе, банкинге и цифровых технологиях"}],["$","link","2",{"rel":"icon","href":"/favicon.ico","type":"image/x-icon","sizes":"16x16"}],["$","$Lf","3",{}]],"error":null,"digest":"$undefined"}
d:"$8:metadata"
10:I[6874,["874","static/chunks/874-437a265a67d6cfee.js","734","static/chunks/app/content/%5B...slug%5D/page-7b3ae3552f08c16e.js"],""]
11:Ta668,<h1>NumPy</h1>
<p>Источник: <a href="https://youtu.be/IWezobwAC-U">https://youtu.be/IWezobwAC-U</a></p>
<p><img src="Screenshot_2022-05-02_at_22.11.46.png" alt="Screenshot 2022-05-02 at 22.11.46.png"></p>
<ul>
<li><strong>Array</strong> — характеристика NumPy-массива</li>
<li><strong>import numpy</strong> — импортируем NumPy</li>
<li><strong>Arrange</strong> — создание массива</li>
<li><strong>Len</strong> — получаем длину</li>
<li><strong>Linespace</strong> — расставляет числовые значения на указанном интервале</li>
<li><strong>Zeros</strong> — заполняем массив пустыми значениями (нулями),</li>
<li><strong>Ones</strong> — заполняем массив единицами</li>
<li><strong>Вектор</strong> — одномерный, однострочный массив</li>
<li><strong>Матрица</strong> — одномерный табличный массив</li>
<li><strong>Куб</strong> — трехмерный массив</li>
<li><strong>Reshape —</strong> делает копию массива и меняет форму</li>
<li><strong>Dot</strong> — перемножение матриц</li>
<li><strong>Shape</strong> — возвращает форму массива</li>
<li><strong>Sum</strong> — возвращает сумму элементов по заданной оси</li>
<li><strong>Axis</strong> — оси 0,1,2</li>
<li><strong>Правила Броадкастинга</strong></li>
<li><strong>Random</strong>  — функция рандомизатор</li>
<li>Printoptions — определяют способ отображения чисел с плавающей точкой</li>
<li><strong>Перемножение 3D и 2D матриц</strong></li>
<li><strong>Деление 3D Матрицы на Вектор</strong></li>
<li><strong>Структурированные массивы</strong> — последовательность именованных полей (с данными любого типа)</li>
<li><strong>Append</strong> — добавление элементов в массив</li>
<li><strong>Stack</strong> — объединение последовательности массивов вдоль конкретной оси</li>
<li><strong>Insert</strong> — вставка элементов в массив</li>
<li><strong>Empty</strong> —  создание пустого массива</li>
<li>Copyto — копирование элементов массива</li>
<li>Delete — удаление элементов массива</li>
<li>Сoncatenate — конкатенация (объединение одного с другим)</li>
<li>Циклы в массивах и матрицах</li>
<li>Split — деление массива на подмассивы по параметру</li>
<li>Ravel — преобразование матрицы в вектор</li>
<li>Flat (итератор) — возвращение копии массива, расплющенного до 1 измерения</li>
<li><strong>Flip</strong> — переворот элементов внутри массива</li>
<li><strong>Roll</strong>  — прокручивание элементов внутри массива</li>
<li><strong>Rot90</strong> — поворот элементов на 90 градусов</li>
<li><strong>Tile</strong> — Повторение последовательности элементов массива n раз</li>
<li><strong>Repeat</strong> — повторение каждого элемента n раз</li>
</ul>
<h2>import библитеки</h2>
<p>Импортируем numpy</p>
<pre><code class="language-python">@import numpy as np
</code></pre>
<h2>Array</h2>
<p>Основа NumPy — это объект ndarray, который используется для представления N-мерного массива, это быстрый и гибкий контейнер для хранения большого набора данных.</p>
<p>Массивы позволяют выполнять математические операции над целыми блоками данных. По сути это обычный list с большим набором возможностей.</p>
<p>У любого numpy-массива (array) есть атрибуты:</p>
<ul>
<li><strong>shape</strong> — кортеж описывающий размер по кадому измерению;</li>
<li><strong>dtype</strong> — объект, описывающий тип данных в массиве;</li>
<li><strong>ndim</strong> — количество измерений массива (одномерный, двумерный, трехмерный)</li>
<li><strong>tolist()</strong> — конвертирует array в обычный python list</li>
</ul>
<p>Array vs List:</p>
<ul>
<li>Array используют меньше памяти, чем списки</li>
<li>Array обладают значительно большей функциональностью</li>
<li>Array, требуют, чтобы данные были однородными</li>
</ul>
<p>Создаем массив</p>
<pre><code class="language-python">my_list = [4, 8, 15, 16, 23, 42]
my_array_from_list = np.array(my_list)
</code></pre>
<p>Умножаем каждый элемент массива на <strong>10</strong></p>
<pre><code class="language-python">my_array_from_list * 10
</code></pre>
<p>Создаем массив из <strong>10</strong> элементов</p>
<pre><code class="language-python">np.arange(10)
np.arange(10, 20)
</code></pre>
<h2>Len</h2>
<p>Получаем длину массива</p>
<pre><code class="language-python">len(np.arange(10, 20))
</code></pre>
<p>Массив с определённым шагом (start, stop, step)</p>
<pre><code class="language-python">np.arange(10, 21, 2)
</code></pre>
<h2>Linespace</h2>
<p>Расставляет числовые значения на указанном интервале через равные промежутки (стартовая позиция / финанльная позиция / колличество мест / шаг)</p>
<p>Нужно расставить 11 стульев в помещении 20 метров</p>
<pre><code class="language-python">np.linspace(0, 30, 11)
</code></pre>
<p>Позволяет узнать количество элементов в массиве</p>
<pre><code class="language-python">np.linspace(0, 30, 11).size
</code></pre>
<p>Узнаем шаг</p>
<pre><code class="language-python">my_linspace = np.linspace(0, 30, 11, retstep=True)
</code></pre>
<p>По сути возвращаемый нам массив linspace состоит из двух элементов это сам массив и шаг</p>
<pre><code class="language-python">my_linspace
# (array([0., 3., 6., 9., 12., 15., 18., 21., 24., 27., 30.]), 3.0)
</code></pre>
<p>Возвращаем второй элемент массива</p>
<pre><code class="language-python">my_linspace[1]
</code></pre>
<h2>Zero</h2>
<p>Используется в тех случаях, когда нужно создать массив и сразу заполнить его какими-то значениями.</p>
<p>Создаём массив и заполняем массив десятью нулями</p>
<pre><code class="language-python">np.zeros(10)
</code></pre>
<h2>Ones</h2>
<p>Для того чтобы создать массив и заполнить его не пустыми значениями, нужно следует использовать функцию <strong>ones.</strong></p>
<p>Создаём массив и заполняем десятью единицами</p>
<pre><code class="language-python">np.ones(10)
</code></pre>
<p>Можно создать не однострочный массив, а подать в него два параметра. В результате мы получим массив из <strong>10</strong> строк и <strong>5</strong> столбцов</p>
<pre><code class="language-python">np.ones((10, 5))
</code></pre>
<p>Преобразуем его в массив массивов. Внутри которого будет 10 массивов, каждый из которых будет состоять из 3-х столбцов и 5 строк. Для этого нужно добавить третий параметр</p>
<pre><code class="language-python">np.ones((10, 5, 3))
</code></pre>
<p>Для того чтобы заполнить массив пустыми строками, нужно использовать следующую конструкцию</p>
<pre><code class="language-python">np.zeros(11, dtype='str')
</code></pre>
<h2>Вектор</h2>
<p>Вектор представляет собой одномерный однострочный массив.</p>
<p>Создадим вектор с числами</p>
<pre><code class="language-python">ave_vector = np.array([4, 8, 15, 16, 23, 42])
</code></pre>
<p>Обратимся к элементу по индексу</p>
<pre><code class="language-python">ave_vector[1] # Второй элемент массива

ave_vector[-1] # Первый элемент с конца
</code></pre>
<p>Так же мы можем присваивать значения конкретному элементу массива. Для примера присвоим второму элементу массива значение <strong>14</strong></p>
<pre><code class="language-python">ave_vector[1] = 14
</code></pre>
<h2>Матрица</h2>
<p>Создаём одномерный массив из <strong>35</strong> элементов</p>
<pre><code class="language-python">ave_array = np.arange(35)
</code></pre>
<p>С помощью функции <strong>shape</strong> распределим значения массива <strong>ave_array между</strong> семью строками и пятью столбцами</p>
<pre><code class="language-python">ave_array.shape = (7,5)
</code></pre>
<p>При обращении к конкретному элементу в таком массиве теперь нам нужно указывать два параметра: строку и столбец</p>
<pre><code class="language-python">row = 5
column = 2
ave_array[row, column]
</code></pre>
<p>Проверяем деление на <strong>2</strong> без остатка. Инструкция ниже говорит о том, что при ее исполнении выведется <strong>True</strong> (если элемент делится без остатка) и <strong>False</strong> (если элемент не делится без остатка)</p>
<pre><code class="language-python">zero_mod_2_mask = 0 == (ave_array % 2)
</code></pre>
<p>В новую <strong>sub_array</strong> мы можем записать те элементы, которые делятся на два без остатка. Для этого достаточно подать нашу переменную <strong>zero_mode_2_mask</strong> в качестве параметра <strong>ave_array</strong></p>
<pre><code class="language-python">sub_array = ave_array[zero_mode_2_mask]
</code></pre>
<p>Можем продолжить дальнейшую фильтрацию и оставить только те значения, которые больше 10 для этого в качестве параметра переменной <strong>sub_array</strong> следует указать параметр <strong>sub_array >10</strong></p>
<pre><code class="language-python">sub_array[sub_array>10]
</code></pre>
<p>Используя аналогичную конструкцию можно отфильтровать значения, которые делятся на 4 без остатка</p>
<pre><code class="language-python">zero_mod_4_mask = 0 == (ave_array % 4)
</code></pre>
<p>И на сам деле можно смело комбинировать эти два условия и с помощью следующей фильтрации вывести элементы, которые делятся и на 2 и на 4</p>
<pre><code class="language-python">combined_mask = np.logical_and(zero_mod_2_mask, zero_mod_4_mask)
</code></pre>
<h2>Куб</h2>
<p>Создадим массив состоящий из 2 массивов, в каждом из которых будет 7 строк и 5 столбцов</p>
<pre><code class="language-python">ave_3D_array = np.arange(70)
ave_3D_array.shape = (2, 7, 5)
</code></pre>
<p>Рассмотрим такой термин как <strong>broadcasting</strong>, используя который в мы можем воспроизводить какие-то действия над элементами массива, при этом воспринимая массив как нечто целое. Давайте например умножим каждый элемент массива на <strong>5</strong> и отнимем <strong>10</strong>.</p>
<pre><code class="language-python">5 * ave_3D_array - 10
</code></pre>
<p>Далее создадим две матрицы <strong>left_mat</strong> и <strong>right_mat</strong> при помощи функции <strong>range</strong>, но на этот раз мы будем использовать <strong>reshape</strong> (он в отличии от <strong>shape</strong> делает копию изначального массива и изменяет его форму, при этом не изменяя оригинальный массив)</p>
<pre><code class="language-python">left_mat = np.arange(6).reshape((2, 3))
right_mat = np.arange(15).reshape((3, 5))
</code></pre>
<p>Далее мы воспользуемся функцией <strong>dot</strong>, что даёт нам перемножение двух матриц. Для этого нам нужно просто передать <strong>left_mat</strong> и <strong>right_mat</strong>  в качестве аргументов</p>
<pre><code class="language-python">dot_product = np.dot(left_mat, right_mat)
</code></pre>
<p>Для того чтобы узнать форму массива следует использовать функцию <strong>shape</strong></p>
<pre><code class="language-python">ave_3d_array.shape
</code></pre>
<h2>Sum</h2>
<p>Мы также можем сложить все элементы массива при помощи функции <strong>sum</strong></p>
<pre><code class="language-python">ave_3d_array.sum()
</code></pre>
<h2>Axis</h2>
<p>Дальше мы применим функцию sum с параметров axis и то чему будет равен этот параметр будет определять то каким образом будет происходить сложение. Например если мы укажем <strong>axis=0</strong>, то первый элемент строки из первой матрицы будет сложен с первым элементов второй матрицы</p>
<pre><code class="language-python">ave_3d_array.sum(axis=0)
</code></pre>
<p>Если же мы поменяем значение параметра <strong>axis</strong> на <strong>1</strong>, то на выходе мы получим двумерный массив, где каждый элемент будет равен сумме столбцов из каждого массива</p>
<pre><code class="language-python">ave_3d_array.sum(axis=1)
</code></pre>
<p>Если же мы поменяем значение параметра <strong>axis</strong> на <strong>2</strong>, то это у будет уже сумма элементов в строках</p>
<pre><code class="language-python">ave_3d_array.sum(axis=2)
</code></pre>
<h2>Правило Broadcasting</h2>
<p>Создадим новый двумерный массив <strong>ave_2d_array</strong>, состоящий из <strong>35</strong> элементов, каждый из которых будет равен <strong>1</strong>. И тип укажем <strong>integer</strong> (то есть целое число). И мы хотим чтобы это была матрица <strong>7</strong> на <strong>5</strong> и каждый из её элементов был умножен на <strong>5</strong></p>
<pre><code class="language-python">ave_2d_array = np,ones(35, dtype='int_').reshape((7, 5)) * 5
</code></pre>
<h2>Random</h2>
<p>Дальше можем использовать функцию <strong>рандомизаторя</strong> для заполнении матрицы. Для этого создадим переменную <strong>ave_random_2d_array</strong>, в которую будет присвоена матрица и рандомно заполнена элементами от <strong>0</strong> до <strong>1</strong></p>
<pre><code class="language-python">ave_random_2d_array = np.random.random(7,5)

</code></pre>
<h2>Printoptions</h2>
<p>Немного усложним задачу и воспользуемся функцией <strong>set_printoptions</strong> и в качестве аргумента передадим <strong>precision</strong> (то есть точность) равную 4 [четыре знака после запятой].</p>
<pre><code class="language-python">np.set_printoptions(precision=4)
</code></pre>
<h2>Перемножение 3D И 2D матриц</h2>
<p>И затем перемножим две матрицы <strong>ave_3d_array</strong> и <strong>ave_random_2d_array</strong>. Не смотря на то, что наши матрицы были разных размеров, нам все таки удалось их перемножить, благодаря правилу <strong>броадкастинга</strong></p>
<pre><code class="language-python">ave_3d_array * ave_random_2d_array
</code></pre>
<p>Правила <strong>броадкастинга</strong> так же работает и с векторами. Например создадим новый вектор <strong>ave_vector</strong> и значение каждого из его элементов умножим на <strong>7</strong>. А после того изменим значение его первого элемента на <strong>-1</strong></p>
<pre><code class="language-python">ave_vector = np.arrange(5) * 7
ave_vector[0] = -1
</code></pre>
<h2>3D матрица / Вектор</h2>
<p>Так же правила <strong>броадкастинга</strong> позволяют делить нашу <strong>3d матрицу</strong> на <strong>вектор</strong></p>
<pre><code class="language-python">ave_3d_array / ave_vector
</code></pre>
<h2>Структурированный массивы</h2>
<p>Такой тип массива примечателен тем, что они организованы как последовательность именованных полей. Причем каждое поле может содержать данные любого типа.</p>
<p>Создадим массив <strong>employee_data_def</strong>, ****который будет содержать в себе четыре кортежа, содержащие в себе название имя и тип данных</p>
<pre><code class="language-python">employee_data_def = [(’name’, ‘s6’), (’height’, ‘f8’), (’wight’, ‘f8’), (’age’, ‘i8’)]
</code></pre>
<p>Давайте создадим еще один массив, который будет состоять по что только из нулей и в котором укаем тип данных <strong>employee_data_def</strong></p>
<pre><code class="language-python">emoloyees_array = np.zeros((4), dtype=employee_data_def)
</code></pre>
<p>Начнем постепенно заполнять наш массив и поменяем работника по индексу <strong>3</strong> на Донателлу, который будет двухметрового роста, солидно весить и жить долго и счастливо</p>
<pre><code class="language-python">emoloyees_array[3] = ('Donatello', 200, 100, 105)
</code></pre>
<p>Добавим ещё одного по индексу ноль</p>
<pre><code class="language-python">emoloyees_array[0] = ('Raphael', 198, 96, 106)
</code></pre>
<p>Давайте посмотрим как выглядит наш массив. И мы увидим, что два сотрудника заполнены, а два пустуют. Кстати, следует обратить внимание, что так как в имени у нас стоит ограничение в <strong>6</strong> символов, то значения обрезались (Donatello → Donate)</p>
<pre><code class="language-python">emoloyees_array
</code></pre>
<p>Кстати мы можем выводит не весь массив целиком, а начиная с определенного индекса. Например если попросить выводить начиная с индекса <strong>1</strong>, то выдача поглотит Рафаэля и мы увидим трех остальных участников</p>
<pre><code class="language-python">employees_array[1:]
</code></pre>
<p>Аналогично мы можем увидеть только возраст наших работников и присвоим переменной ages выдачу по age. И в результате мы получим массив, который состоит из четырех элементов коррелирующих с возрастом наших работников</p>
<pre><code class="language-python">ages = employees_array['age']
</code></pre>
<p>А теперь давайте создадим массив немного побольше и назовем его <strong>company_employees_all</strong>, который пока что заполним нулями и будет иметь форму <strong>4</strong> на <strong>3</strong> на <strong>2</strong> и его тип данных будет также равен начешу типу данных кортежами. Как мы видим получилось все тоже самое, только в гораздо большем масштабе</p>
<pre><code class="language-python">company_employees_all = np.zeros((4, 3, 2)), dtype = employess_data_ref
</code></pre>
<p>И теперь чтобы занести некоторого работника, нам придется указывать целых три координаты. Скажем мы хотим добавить некоторого человека <strong>Pipin</strong> и для этого нужно будет поступить следующим образом, мы указываем индекс куда мы хотим записать данные человека и непосредственно сами данные</p>
<pre><code class="language-python">company_employees_all[3, 2, 1] = ('Pipin', 90, 120, 98)
</code></pre>
<p>И так же можно увидеть, что если мы запросим выборку по конкретному параметру, то она будет иметь структура матрицы c единственным заполненным элементом — рост Пипина</p>
<pre><code class="language-python">company_employees_all['height']
</code></pre>
<p>Аналогичным образом можно запросить и рост и вес и получить следующий результат, в котором добавиться просто одни параметр</p>
<pre><code class="language-python">company_employees_all['height', 'weight']
</code></pre>
<h2>Append</h2>
<p>Для следующих действий нам понадобится массив, который будет состоять из 24-х элементов и придадим ему форму <strong>2</strong> на <strong>3</strong> на <strong>4</strong></p>
<pre><code class="language-python">ave_array = (np.arrange(24)).reshape(2, 3, 4)
</code></pre>
<p>Давайте посмотрим как мы им можем манипулировать и добавлять новые элементы. Для этого создадим массив <strong>trail_ave_array</strong> и присвоим ему значения.  Для этого воспользуемся функцией <strong>append</strong> в которую в качестве аргумента передадим наш нынешний массив <strong>ave_array</strong> и через запятую то, что мы хотим к нему присоединить. Пусть это будет последовательность чисел [5, 6, 7, 8]</p>
<pre><code class="language-python">ave_array = np.append(ave_array, [5, 6, 7, 8])
</code></pre>
<p>Если мы не любим всё плоское и одномерное, то мы можем использовать функцию <strong>reshape</strong> c аргументами равными форме, которую мы можем дать элементам (пусть это будет <strong>7</strong> строк и <strong>4</strong> столбца между которыми равномерно распределяются наши элементы)</p>
<pre><code class="language-python">trial_ave_array.reshape[(7,4)]
</code></pre>
<p>Рассмотрим ещё одну конструкцию благодаря которой мы можем создавать массивы и на ходу их видоизменять. Для этого создадим новую переменную <strong>new_trial_array</strong> в которую присвоим новый массив используя конструкцию np.array, в который передадим еще одну функцию <strong>arrange</strong> на <strong>24</strong> элемента и через reshape предадим ей форму <strong>2</strong> на <strong>3</strong> на <strong>4</strong> при этом каждый элемент умножаем на <strong>10</strong> и прибавляем ему <strong>5</strong></p>
<pre><code class="language-python">new_trial_array = np.array(np.arrange(24)).reshape(2,3,4) * 10 + 5
</code></pre>
<p>Изменение формы с помощью управления осями</p>
<pre><code class="language-python">np.append(ave_array, new_trial_array, axis = 1)
</code></pre>
<h2>Stack</h2>
<p>Если же вы немного путаетесь, что такое ось 1, что такое ось ноль, то для этого есть более интуитивный функционал, а именно функция <strong>stack</strong>. Она бывает двух типов <strong>hstack</strong> — горизонтально [поставит одну матрицу на другую] и <strong>vstack</strong> — вертикально. В качестве аргументов функция принимает один и второй массив</p>
<pre><code class="language-python">ave_stack = np.hstack((ave_array, new_trial_array))
</code></pre>
<h2>Insert</h2>
<p>Для того чтобы вставит массив, можно воспользоваться функцией insert и в параметрах указать: куда будет происходить вставка, какой индекс, что именно будем вставлять и по какой оси. Данна функция не заменит элемент на новый, а именно вставит его во внутрь массива.</p>
<pre><code class="language-python">insert_ave_array = np.insert(ave_array, 1, 666, axis=0)
</code></pre>
<h2>Empty</h2>
<p>Создадим пустой массив с помощью функции empty, который будет копировать форму <strong>ave_array</strong>,</p>
<pre><code class="language-python">del_array = np.empty(ave_array.shape)

</code></pre>
<h2>Copyto</h2>
<p>А дальше используя функцию copyto мы скопируем в массив <strong>del_array</strong> содержание <strong>ave_array</strong></p>
<pre><code class="language-python">np.copyto(del_array, ave_array)
</code></pre>
<h2>Delete</h2>
<p>Теперь мы можем начать работать с удалением. Для этого мы используем функцию <strong>delete</strong> и в параметрах укажем: где мы производим удаление, какой элемент и по какой оси. И в результат выполнения этой операции мы избавились от второго элемента, который представлял из себя массив</p>
<pre><code class="language-python">np.delete(del_array, 1, axis=0)
</code></pre>
<p>Если мы поменяем значение <strong>axios</strong> на <strong>1,</strong> то удаление произойдет во втором столбце во всех вложенных массивах</p>
<pre><code class="language-python">np.delete(del_array, 1, axis=1)
</code></pre>
<h2>Конкатенация</h2>
<p>Теперь посмотрим как работает конкатенация. Для этого организуем массив <strong>alpha</strong> и используя функцию array мы заполним его элементами. Далее организуем ещё один массив <strong>beta</strong>, так же заполним его элементами начнем практиковать конкатенация. Сама по себе конкатенация — это объединение одного с другим. В нашем случае <strong>alpha</strong> является матрицей, а <strong>beta</strong> является вектором, тем не менее мы можем из без проблем объединить. Для этого создадим переменную <strong>together</strong> и используя функцию <strong>contcate</strong> укажем два наших массива и ось (в нашем случае это 0[строки]). И по результату мы получаем матрицу, в которой объединяются <strong>alpha</strong> и <strong>beta</strong>.</p>
<pre><code class="language-python">together = np.contacte((alpha, beta) axis=0)
</code></pre>
<p>Давайте проверим параметры нашей матрицы при помощи функции <strong>shape</strong> и по результату мы имеем <strong>3</strong> строки и <strong>2</strong> столбца</p>
<pre><code class="language-python">together.shape
</code></pre>
<p>Как вы помните мы также можем присваивать элементам матрицы отдельные значения. Для этого возьмём нашу матрицу <strong>together</strong> и элементу, который в находится во второй строке, второго столбца присвоим значение <strong>666</strong></p>
<pre><code class="language-python">together[1, 1] = 666
</code></pre>
<h2>Циклы</h2>
<p>Дальше давайте посмотрим как нам могут пригодится циклы в случае с массивами. Для этого создадим новую переменную <strong>arrays</strong> и присвоим в неё массив заполненный нулями. Он у нас будет формой <strong>5</strong> на <strong>3</strong> на <strong>4</strong>. И дальше мы пишем совершенно стандартный цикл for. И указываем, что в интервале от нулевого элемента до четвёртого включительно мы присваиваем всем элементам рандомное значение.</p>
<pre><code class="language-python">arrays = np.zeros((5, 3, 4))
for n in range(5):
	arrays[n] = np.random.randn(3, 4)

</code></pre>
<p>Теперь видно как мы можем итерировать по массиву и последовательно указывать элементам рандомные значения</p>
<pre><code class="language-python">arrays
</code></pre>
<p>Разберемся с функцией <strong>stack</strong>. Для этого организуем новую переменную и истользуем функцию stack, которая будет соединять поcледовательность элементов массива по какой-то определенной оси (<strong>axis = 0</strong> — строки, <strong>axis = 1</strong> — столбцы, <strong>axis = 2</strong> — ...)</p>
<pre><code class="language-python">stack0 = np.stack(arrays, axis = 0)
stack1 = np.stack(arrays, axis = 1)
stack3 = np.stack(arrays, axis = 2)
</code></pre>
<h2>Split</h2>
<p>Функция <strong>split</strong> разделяет наш массив на некоторые подмассивы, по некоторому параметру. Для этого сложим наш созданный массив в переменную <strong>before</strong> сплит. Затем создадим переменную <strong>s0</strong> в которую сложим результат после сплитирования и непосредственно вызовем функцию <strong>split</strong> в которой укажем три параметра: что разделяем, на сколько частей и по какой оси</p>
<pre><code class="language-python">s0 = stack0(before_split, 5, axis=0)
</code></pre>
<p>Давайте еще создадим один массив s1 и попробуем сделать сплитирование немного с другими параметрами. Разделим <strong>before_split</strong> на <strong>3</strong> части внутри по оси <strong>1</strong></p>
<pre><code class="language-python">s1 = stack0(before_split, 3, axis=1)
</code></pre>
<h2>Ravel</h2>
<p>Функция <strong>ravel</strong> позволяет преобразовать <strong>матрицу</strong> в <strong>вектор</strong>. Для на начала создадим новый массив <strong>ave_start_array</strong>, который будет состоять из <strong>24</strong> элементов. Затем разделим с помощью функции reshape разделим его на 3 части и результат сложим в <strong>ave_3_8_array</strong>. Затем давайте преобразуем его в плоский список. Для этого применим к нему функцию <strong>ravel</strong> и результат сложим в <strong>ave_ravel_array</strong>. Давайте выведем его на экран и убедимся, что он равен нашему стартовому массиву.</p>
<pre><code class="language-python">ave_start_array = np.array(np.arrange(24))
ave_3_8_array = ave_start_array.reshape((3, 8))
ave_ravel_array = ave_3_8_array.ravel()
</code></pre>
<h2>Flat</h2>
<p>Так же есть функция <strong>flat</strong>, которая проходит по массиву и возвращает его сплющенную копию. Для это создадим цикл и применим функцию <strong>flat</strong></p>
<pre><code class="language-python">ave_start_array = np.array(np.arrange(24))

for n in ave_3_8_array flat
	print(n)
</code></pre>
<h2>Flip</h2>
<p>Мы можем пререгруппировывать элементы внутри массива. Возьмем наш массив <strong>ave_3_8_array</strong> и передадим его в качестве аргумента в функцию <strong>fliplr</strong>  и мы увидим, что элементы всех строк просто перевернуло в обратном порядке</p>
<pre><code class="language-python">
np.fliplr(ave_3_8_array)
</code></pre>
<p>Если применить к функцию <strong>fliplr</strong> к многомерному массиву, то она поменяет столбцы с низу вверх</p>
<pre><code class="language-python">ave_2_4_4_array = ave_3_8_array.reshape(2, 4, 4)
np.fliplr(ave_2_4_4_array)
</code></pre>
<p>Если мы хотим провернуть аналогичную операцию  в матрице, то. мы должны использовать функцию <strong>flipud</strong></p>
<pre><code class="language-python">mp.flipud(ave_3_8_array)
</code></pre>
<h2>Roll</h2>
<p>помимо переворачивания элементов массива мы можем их прокручивать. Для этого в <strong>numpy</strong> существует функция <strong>roll</strong>. То есть мы можем начать с определенного индекса и грубо говоря перекрутить нужные нам элементы массива в другую сторону. Для этого возьмем в функцию <strong>roll</strong> передадим переменную <strong>ave_start_array</strong> и вторым параметром указываем сдвиг на <strong>5</strong> элементов. И по результату мы видим, что <strong>5</strong> последних элементов были добавлены как бы спереди массива.</p>
<pre><code class="language-python">np.roll(ave_start_array, 5)
</code></pre>
<p>И аналогично если мы будем использовать отрицательный сдвиг <strong>-5</strong>, то первые пять элементов будут перемещенный в конец массива в том же порядке, что они и стояли сначала. Теперь посмотрим что будет если мы применим ту же функцию скажем на трёхмерном массиве</p>
<pre><code class="language-python">np.roll(ave_start_array, -5)
</code></pre>
<p>Посмотрим, что будет если применить туже функцию на трехмерном массиве. Для этого возьмем ранее созданный <strong>ave_2_3_4_array</strong>  со сдвигом равным <strong>2</strong>. И мы увидим как последние два элемента из второго вложенного массива переползли в начало первого</p>
<pre><code class="language-python">np.roll(ave_start_array, 5)
</code></pre>
<h2>Rot90</h2>
<p>Помимо прокрутки мы можем использовать функцию поворота на 90 градусов. Для этого применим функцию <strong>rot90</strong> и в качестве аргумента подадим наш массив <strong>ave_3_8_array</strong>. И по результату исполнения функции мы видим как из формы в <strong>3</strong> строки и <strong>8</strong> столбцов он фактически развернулся и принял форму в <strong>8</strong> строк и <strong>3</strong> столбца</p>
<pre><code class="language-python">np.rot90(ave_3_8_array)
</code></pre>
<h2>Tile</h2>
<p>Следующая на очереди у нас функция <strong>tile</strong>. На самом деле она просто повторяет повторяет последовательность элементов в соответсвии с из параметрами указанное количество раз. Чтобы это проверить создадим новый массив <strong>ave_array_one</strong> с десятью элементами. Передадим его в качестве параметра в функцию tile и вторым параметром укажем, что хотим повторить <strong>5</strong> раз</p>
<pre><code class="language-python">ave_array_one = np.array(np.arrange(10))
np.tile(ave_array_one, 5)
</code></pre>
<p>И с помощью функци <strong>reshape</strong> мы можем добавить измерений и сделать наш массив <strong>5</strong> на <strong>10</strong></p>
<pre><code class="language-python">np.tile(ave_array_one, 5).reshape(5, 10)
</code></pre>
<h2>Repeat</h2>
<p>И в завершении рассмотрим функцию <strong>repeat</strong>. Её отличие от <strong>tile</strong> заключается в том, что она повторяет не всю последовательность элементов несколько раз, а каждый элемент <strong>N</strong> число раз. То есть если мы вызовем repeat, подадим в качестве параметра <strong>ave_array_two</strong> и укажем число повторений <strong>3</strong>, то мы увидим как каждый элемент массива повторится по <strong>3</strong> раза</p>
<pre><code class="language-python">ave_array_two = np.array(np.arrange(7))
np.tile(ave_array_two, 3)
</code></pre>
4:["$","div",null,{"className":"max-w-4xl mx-auto p-8","children":[["$","div",null,{"className":"mb-6","children":["$","$L10",null,{"href":"/","className":"text-blue-600 hover:underline","children":"← Назад на главную"}]}],["$","article",null,{"className":"prose prose-lg max-w-none","children":["$","div",null,{"dangerouslySetInnerHTML":{"__html":"$11"}}]}]]}]
